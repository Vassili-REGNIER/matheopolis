---
description: CI and workflow pointer
globs: ".github/**/*.yml"
alwaysApply: false
---

# CI workflow

Use `/.ia/workflow-quality.md`.

Mandatory:

- Split quality checks into independent jobs.
- Keep CI and docs aligned when quality workflow changes.
