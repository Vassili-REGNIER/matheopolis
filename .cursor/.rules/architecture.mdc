---
description: Backend architecture pointer
globs: backend/src/**/*.php
alwaysApply: false
---

# Backend architecture reference

Use `/.ia/backend-architecture.md` as canonical guidance.

Hard rules:

- Keep dependency direction clean (Domain <- Application <- Adapter/Infrastructure).
- Do not keep dead legacy code after refactors.
- Keep backend API-first and session-based.
