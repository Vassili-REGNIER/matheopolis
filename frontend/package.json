{
  "name": "matheopolis-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend TypeScript vanilla de Matheopolis",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
